// The desktop sidebar transition lasts 300 ms. The shared report chart container holds its
// current SVG size during that motion and applies the latest observed size once at the end.
export const REPORT_CHART_RESIZE_DEBOUNCE_MS = 320
